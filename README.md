## Prerequisites
- Make
- MinGW
- clang-format (using LLVM on windows)

## Recomended
- Clangd
    - Run with arguments:
        - `--enable-config`
        - `-log=verbose`
        - `-pretty`
        - `--background-index`